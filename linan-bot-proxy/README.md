# 林安数字分身 - GLM API 代理

Vercel Edge Function 代理，用于转发前端请求到智谱 GLM API。

## 部署

1. 将本项目推送到 GitHub
2. 在 Vercel 导入该仓库
3. 自动部署完成

## API 地址

部署后访问：`https://你的项目名.vercel.app/api/chat`

## 前端配置

将前端 HTML 中的 `PROXY_URL` 替换为上述地址。
